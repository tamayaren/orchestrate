{
  "name": "orchestrate",
  "version": "1.0.0",
  "private": true,
  "description": "Record and apply local coding-agent profiles across Windows, macOS, and Linux.",
  "type": "module",
  "bin": { "orchestrate": "./bin/orchestrate.js" },
  "scripts": { "test": "node --test", "check": "node --check bin/orchestrate.js && node --check src/orchestrate.js" },
  "engines": { "node": ">=20" }
}
